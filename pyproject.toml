[project]
name = "quiniela2026"
version = "0.1.0"
description = "Pipeline modular para pronosticos de quiniela del Mundial 2026."
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "torch>=2.3"
]

[tool.quiniela]
database_path = "data/quiniela.db"
